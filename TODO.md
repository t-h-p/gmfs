gmfs (graph my filesystem)

start from current (or specified if -d flag) directory -- make a fun graph of entire filesystem from there

uses -- libgvc (graphviz), dirent.h to get directories and such



